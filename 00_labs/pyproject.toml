[project]
name = "00-labs"
version = "0.1.0"
description = "This is a collection of labs that showcases how to create deep research agents."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=6.30.1",
    "pydantic-ai>=0.8.1",
]
